<%= render partial: "devise/shared/headerdevise" %>
<div class="panel-heading">
    <h3 class="panel-title">Recuperar senha</h3>
</div>

<div class="panel-body">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= devise_error_messages! %>

    <form role="form">
        <fieldset>
            <div class="form-group">
                <%= f.email_field :email, autofocus: true, class:"form-control", placeholder:"E-mail" %>
            </div>

            <%= f.submit "Enviar instruções para recuperar", class:"btn btn-lg btn-success btn-block" %>
        </fieldset>
    </form>
    <% end %>

    <%= render "devise/shared/links" %>
</div>
<%= render partial: "devise/shared/footerdevise" %>